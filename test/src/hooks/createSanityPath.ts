import { SANITY_PROJECT_ID, SANITY_DATASET } from '$env/static/private';

const domain = https://cdn.sanity.io/images/

export const createImaePath = (asset) => {
    const url = domain + SANITY_PROJECT_ID + '/' 'SANITY_DATASET'
}