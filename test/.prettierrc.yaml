# https://github.com/xojs/xo/issues/542 (によってprettierをxoでは設定しない)

# singleQuote: true
semi: false
plugins:
    - "prettier-plugin-svelte"
overrides:
    -
        files:
            "*.svelte"
        options:
            parser:
                "svelte"